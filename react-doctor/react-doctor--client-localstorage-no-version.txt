Rule: react-doctor/client-localstorage-no-version
Severity: warning
Category: Correctness
Count: 1

sessionStorage.setItem("processedSymbols", JSON.stringify(...)) — bake a version into the key (e.g. "processedSymbols:v1") so a future schema change can ignore old data instead of crashing on it

Suggestion: Bake a version into the storage key (e.g. "myKey:v1"); a future schema change can ignore old data instead of crashing on it

Files:
  src/pages/ProcessingPage.tsx:262
