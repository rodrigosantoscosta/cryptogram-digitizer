Rule: react-doctor/rerender-functional-setstate
Severity: warning
Category: Performance
Count: 1

setOptions({ ...options, ... }) — use functional update `setOptions(prev => ({ ...prev, ... }))` to avoid stale closures

Suggestion: Use the callback form: `setState(prev => prev + 1)` to always read the latest value

Files:
  src/examples/processing-example.tsx:60
